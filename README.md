# leads-priorizacion

Sistema de priorización de leads para MotosMotores S.A.S.

## Instalación

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux / macOS
source .venv/bin/activate

pip install -r requirements.txt
```
